from dataclasses import dataclass
import httpx


class Course(models.Model):
    
    CIN
    CSesudo /
class Report:
    course = 
    c
    scope: dict = {'Quarterly': 'q', 'Fiscal Year': 'fy'} 
    page: list = 

    def session_context(self, report):
        with httpx.Client() as client:

